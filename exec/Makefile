# Synchronet Baja Module Makefile (for Borland make)

# Requires Baja v2.20+

# @format.tab-size 8, @format.use-tabs true

# $id$

all : 	bullseye.bin \
        chat_sec.bin \
	cntnodes.bin \
	default.bin \
	dir.bin \
	file_io.bin \
        ftp.bin \
        getimlst.bin \
	login.bin \
	logon.bin \
	major.bin \
	matrix.bin \
	mudgate.bin \
	noyesbar.bin \
	pcboard.bin \
        qnet-ftp.bin \
	ra_emu.bin \
	renegade.bin \
	rlogin.bin \
	sdos.bin \
	simple.bin \
        socktest.bin \
	str_cmds.bin \
	telgate.bin \
	type.bin \
	typehtml.bin \
	unixgate.bin \
	wildcat.bin \
	wiplogin.bin \
	wipshell.bin \
	wwiv.bin \
	yesnobar.bin 

.src.bin:
	@baja /q $<
