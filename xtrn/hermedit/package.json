{
  "name": "hermedit",
  "version": "0.1.0",
  "private": true,
  "description": "Synchronet-first BBS message editor with CP437 art tools (TypeScript, compiled to ES5 for SpiderMonkey 1.8.5)",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc -p tsconfig.build.json && node scripts/bundle.mjs",
    "test": "vitest run",
    "test:watch": "vitest",
    "smoke": "npm run build && /sbbs/exec/jsexec dist/smoke_runner.js",
    "check": "npm run typecheck && npm run test && npm run smoke"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "tslib": "^2.8.0",
    "typescript": "~5.9.3",
    "vitest": "^3.0.0"
  }
}
