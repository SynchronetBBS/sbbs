change to src/ and type "make"
copy syncdraw to ~/bin, /usr/local/bin or somewhere else in your path.
Create ~/.syncdraw and copy syncdraw.config and allfont.fnt to this location

now you can type "syncdraw" and start the program.

(Don't forget to rehash if you're using tcsh)
