# build output
/build/
/build-msvc/
/build-test/
/build-probe/
/syncretro
/syncretro.map
*.o
# libretro cores, console BIOS, and game ROMs are runtime content, never
# committed (sysop-supplied -- see PROVENANCE.md). NOT libretro.h, which IS
# vendored and tracked.
*_libretro.so
*_libretro.dll
*.bin
*.rom
# MSVC build byproducts
*.pdb
*.ilk
*.exp
# editor swap
*.swp
*.swo
